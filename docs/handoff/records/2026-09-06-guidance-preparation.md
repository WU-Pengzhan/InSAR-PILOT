# 2026-09-06｜五页基线确认与交接准备

[交接总索引](../index.md) · [当前交接](../current.md) · [任务提示词](../prompts.md)

阶段：指导/实施准备，已完成。不是 P01—P05 任一页面的设计或实现验收。
用户指令：同意此前页面意见，修改指导方针，做好准备，提供每页 prompt 并维护工作交接索引。

## 决定与范围

- 已确认五页职责、参数/生成子视图、成果/QC 同页和运行中的逐步基础检查。
- Web only、Sentinel first，停止 Qt 界面开发，NISAR 新工作及昂贵科学评价暂缓。
- phase stack 具体产品未被这次页面同意隐式确定；继续记录为索引 D01。
- 本次只更新文档，无产品代码、安装依赖、默认入口或科学参数改动。

## 完成的准备

- AGENTS/CONTRIBUTING 要求任务从交接索引进入，并在结束时写回记录与状态。
- 架构、页面文档、README/站点入口的五页状态由提案改为已确认。
- 建立 P01—P05 任务卡：目标、输入/输出、范围、依赖、代码/测试入口、验收重点及最新交接。
- 建立设计提示词和实现/继续/只验收模板；指定单页范围，避免整套页面一次实现。
- 建立交接索引、当前环境记录和单次交接模板；旧记录追加保留。
- 14 份被更新的指导文件原样归档并校验，归档目录为
  `archive/guidance/2026-09-06-page-handoff-baseline/`；不是全仓库备份。

## 验证

- Web 环境执行 `python -m mkdocs build --strict --site-dir /tmp/insar-pilot-handoff-docs`：通过。
- WSL 仓库执行 `git diff --check`：通过。
- 五张任务卡中 66 条代码/测试路径引用存在；14 份归档 SHA-256 一致，无嵌套生效 AGENTS.md。
- 手工/链接检查覆盖交接入口、页面卡、提示词和架构状态；最终文档检查结果同步 migration。
- 未运行页面软件测试、浏览器 E2E、真实下载或科学计算；旧测试结果不作为本轮结果。

## 服务与数据

没有启动或停止用户服务、下载、科学 worker 或浏览器测试服务。
实时任务状态未探测，后续任务按需重新检查。只核对记录中的本机可执行文件/缓存路径存在。
既有工作树修改、科学输入和黄金数据保留，未记录 token 或凭据。

## 下一步

用户可发出 P01 设计提示词。任务先读根 AGENTS、交接索引/current 和 P01 卡，再核对代码。
完成本页审阅设计后写入新的 records 文件，更新 P01 卡与总索引；不自动实施 P02。
如果用户明确要求直接实现其已认可设计，则按授权完成，不再增加重复批准流程。
