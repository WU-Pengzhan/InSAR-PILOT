# 公共外壳 / P01｜1.5.0 发行完成检查

日期：2026-09-08（接续 2026-09-07 同一任务）。
用户指令：继续文档优化、残余清理、打包与 GitHub main / 1.5.0 发布；README 以界面与功能介绍为重点。
阶段：修复、验收、发行。
任务卡：[公共外壳](../pages/shell-project-entry.md)、[P01](../pages/p01-search-download.md)。
上一份：[文档与清理](2026-09-07-v1.5.0-release.md)，原样保留。

## 接受的决定与仍未知的事项

- 沿用已授权 Web 发行与 Qt 清理范围，不扩展五页或决定 phase stack D01。
- 2026-09-08 接续时工作树干净，昨日内容已由外部提交为 5d2fac9 并推至 origin/main；在其上追加完成检查，不重写该提交。
- P01 真实账号长时下载、P02—P05 专业页面、NISAR 新 UI 与完整科学验证仍独立待办。

## 实际完成

- 接续已完成的产品 README 中英、真实 Web 截图、分层用户文档、85 个 Qt 专属源码文件清理、依赖/入口 Web 化、wheel/sdist 构建与检查。
- 修复窗口已断开时 close 再抛 WebSocketDisconnect 的日志异常；补充断开后释放与接续回归。测试服务降为 warning 日志，避免输出 URL 中临时窗口身份。
- 远端 CI 暴露 GDAL 测试依赖未安装、Python 3.12 下 NumPy stub 与 mypy 3.10 target 不匹配，工作流补齐 gdal-bin/aria2 并按 Python 矩阵设置类型目标。
- 中文文件选择测试使用当前“保存到（父文件夹）”标签。工程打开请求尚未返回时切换页面会被后续打开结果抢回的竞态已修复：在用户发起打开时冻结导航序号；等待测试改为在最早的 open 请求阶段阻塞，验证后续导航被保留。
- 清理误入检查点提交的两个自建 .codex_tmp 文件并忽略临时目录；CITATION 的格式版本保持 1.2.0，软件版本为 1.5.0，发行日期统一为 2026-09-08。
- 更新前 8 份指导/引用原文与摘要归档至 archive/guidance/2026-09-08-before-release-finalization；之前 69 份归档与删除清单保留。

## 验证与证据

- 昨日 Web Python 无 Qt 完整回归 470 passed；新增关闭异常修复后 window/API/launcher 定向 17 passed，不能合并为一次完整运行。
- 前端 20 单测与 Vue 类型/生产构建通过；Ruff 与对应 mypy 通过。
- 昨日 Linux Firefox/Chromium 检索、下载、工程、单窗口 18 passed / 2 Windows-only skipped。Windows Edge→WSL 相同范围 9 passed / 1 独立 Chrome 用例 skipped；后者受用例环境开关控制，未算通过。
- Codex Windows 浏览器→WSL 实图检查中英、主题、无工程首页、AOI/真实公共底图；1440×1000 与默认视口检查。README 图片包含示例区域，不伪造下载或科学结果。
- 初次 Linux 浏览器尝试因缓存缺少对应执行文件失败，安装官方 Playwright 浏览器后上述回归通过。
- wheel 曾在独立 Python 3.11 环境成功安装：无 PySide6，默认命令、模块入口、两个旧 CLI help、静态页、1.5.0 health、启动/status/stop 均通过。最终重新打包与远端结果在下方记录。
- MkDocs strict、138 条修改文档本地链接及 69 份归档摘要曾通过，最终文档再次检查。
- 未做真实认证长时传输、SAR/黄金计算、完整 OS 缩放/睡眠长时测试；不改科学输入、参数、历史运行与成果。

## 服务与任务

- 接续实测正式服务 8765 RUNNING，0 下载/处理/worker。正式服务未停止，截图检查页已关闭。
- 昨日 wheel 独立服务 18765 已显式 stop。Windows fixture PID 99035 已核对命令后 SIGTERM。
- 本日浏览器测试由 Playwright 管理独立 8767 fixture，最终退出检查与发布状态在下方记录。

## 下一步

检查本记录的最终结果与 GitHub Release 后，按下一条用户指令开展 P01 真实下载验收或 P02 设计。D01 在依赖工作前确认，不自动进入后续页面。
索引、current、公共/P01 卡、migration 已更新至本记录。

## 最终本地检查

- 本日文件选择与外壳布局 Linux Firefox/Chromium 20 passed；前端单测 20 passed、类型/生产构建通过。Ruff、MkDocs strict、git diff --check 通过。
- 清理构建缓存后重新生成 wheel/sdist，发行结构检查通过；独立 Python 3.11 重装最终 wheel，依赖/静态资源检查、HTTP 首页、启动/status/stop 通过。未认证 API 请求正确返回 401；status 使用应用认证检查存活。独立 18765 服务已停止。
- 两批指导归档摘要再次全部核对通过。远端 CI 与公开发行待推送后核验，完成后追加独立发布回执，不提前宣称发布成功。
