name: isce-master
dependencies:
  - python>=3.10,<3.13
  - pip
  - isce2
  - numpy
  - gdal
  - pytest>=7.4
  - ruff
  - build
  - pip:
      - pyside6>=6.6
      - pyshp>=2.3

# Notes:
# 1) This environment installs GUI/runtime essentials + ISCE2.
# 2) After creating/activating this env, install the app from repository root:
#    pip install .
